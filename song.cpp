#include <QString>
#include "song.h"

Song::Song(std::wstring& str) {
    auto qstr = QString::fromStdWString(str);
    auto qlist = qstr.split(qstr,)
    for (size_t i = 0; i < str.size(); ++i) {

    }
}
